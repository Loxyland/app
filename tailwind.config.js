module.exports = {
    theme: {
      extend: {
        colors: {
          // Untuk mengakses variabel CSS
          bg: 'var(--bg-color)',
          headings: 'var(--headings-color)',
          desc: 'var(--desc-color)',
        },
      },
    },
    variants: {},
    plugins: [],
  }